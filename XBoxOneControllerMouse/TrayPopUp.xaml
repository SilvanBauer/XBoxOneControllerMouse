<UserControl x:Class="XBoxOneControllerMouse.TrayPopUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XBoxOneControllerMouse"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="Chartreuse" CornerRadius="4">
            <StackPanel>
                <Button Content="Settings" Margin="5 5 5 0" Click="Settings" />
                <Button Content="Exit" Foreground="DarkRed" Margin="5" Click="Exit" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
